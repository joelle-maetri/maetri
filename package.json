{
  "name": "maetri",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "build-prod": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
    "serve": "live-server --port=3800",
    "dev": "npm run build & npm run serve",
    "deploy": "npm run build-prod && cp dist/output.css styles.css && gh-pages -d .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.11",
    "autoprefixer": "^10.4.21",
    "gh-pages": "^6.3.0",
    "live-server": "^1.2.2",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.11"
  }
}
